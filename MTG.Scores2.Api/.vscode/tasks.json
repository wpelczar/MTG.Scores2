{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/MTG.Scores2.Api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}